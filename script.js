const menuBtn=document.querySelector(".menu-btn");const navLinks=document.querySelector(".nav-links");
function closeMenu(){navLinks.style.display="";menuBtn.textContent="☰";menuBtn.setAttribute("aria-label","Open menu")}
function openMenu(){navLinks.style.display="flex";navLinks.style.position="absolute";navLinks.style.top="76px";navLinks.style.left="0";navLinks.style.right="0";navLinks.style.padding="18px 20px";navLinks.style.background="#f6f5f1";navLinks.style.flexDirection="column";navLinks.style.borderBottom="1px solid #e5e2da";menuBtn.textContent="✕";menuBtn.setAttribute("aria-label","Close menu")}
menuBtn?.addEventListener("click",()=>{const open=navLinks.style.display==="flex";open?closeMenu():openMenu()});
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",()=>{if(window.innerWidth<=900)closeMenu()}));
document.addEventListener("click",e=>{if(window.innerWidth<=900&&navLinks.style.display==="flex"&&!navLinks.contains(e.target)&&!menuBtn.contains(e.target))closeMenu()});